{
  "swagger": "2.0",
  "info": {
    "title": "Test API",
    "description": "Test API",
    "version": "1.0.0"
  },
  "host": "jsonplaceholder.typicode.com",
  "schemes": [
    "https"
  ],
  "basePath": "/",
  "produces": [
    "application/json"
  ],
  "paths": {
    "/posts": {
      "get": {
        "summary": "Get Posts",
        "description": "Returns all posts or all posts by an user.",
        "parameters": [
          {
            "name": "userId",
            "in": "query",
            "description": "ID of an exiting user.",
            "required": false,
            "type": "number"
          }
        ],
        "tags": [
          "Posts"
        ],
        "responses": {
          "200": {
            "description": "An array of posts",
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Post"
              }
            }
          }
        }
      }
    }
  },
  "definitions": {
    "Post": {
      "properties": {
        "userId": {
          "type": "string",
          "description": "Id of the user that created the post."
        },
        "id": {
          "type": "string",
          "description": "Post id."
        },
        "title": {
          "type": "string",
          "description": "Displays the title of post."
        },
        "body": {
          "type": "string",
          "description": "Body of the post."
        }
      }
    }
  }
}