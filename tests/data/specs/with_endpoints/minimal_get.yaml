api:
  base_url: https://jsonplaceholder.typicode.com/
  endpoints:
    - namespace: posts
      path: /posts
      requests:
        - name: list_all_posts
          method: get
          tests:
            - status_code_is_200: response.status_code == 200
